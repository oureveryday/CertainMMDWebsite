# LOL

```javascript
...
console[n(923)]("unpack gzip and parse json");
                          const buffer = new Uint8Array(u);
                          const blob = new Blob([buffer], { type: 'application/gzip' });
                          const link = document.createElement('a');
                          link.href = URL.createObjectURL(blob);
                          link.download = 'pmx.gz';
                          document.body.appendChild(link);
                          link.click();
                          document.body.removeChild(link);
                        var h =
...
```

```javascript
...
 _0x1ff93a = new Date();
                      const blob0x1146b = new Blob([_0x1146b], { type: 'application/gzip' });
                      const link0x1146b = document.createElement('a');
                      link0x1146b.href = URL.createObjectURL(blob0x1146b);
                      link0x1146b.download = 'motion.vmd';
                      document.body.appendChild(link0x1146b);
                      link0x1146b.click();
                      document.body.removeChild(link0x1146b);
                    console[_0x1fbecd(894)](
...
```
